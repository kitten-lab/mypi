<?php
/**
 * SYS www · DOM public · ROOM home
 * Restored browser-surface — sky only in m/.
 */
SKY__AUTH(
    'browser', 'Browser MOD',
    'public', 'Public web',
    'home', 'Home — Inter-Network',
    'classic'
);

openSky(ROOM_DISPLAY);
medHeading('Welcome to WWW');
leaf("This is the pocket **browser surface** — restored from archive ideas (DEMO/WWW + explorer chrome). You are inside a window that *pretends* to be the web, on your own machine.");
leaf("Use the address bar (webBAR kitten), side rooms, and the **Tools shelf** below the main page. Soft-string install is the dream; today doors call getTool.");
hr();
leaf("Try: Shelves → Tools · ROMs → Toys · or jump to Starline News from the footer.");
closeSky();
