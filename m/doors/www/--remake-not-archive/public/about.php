<?php
SKY__AUTH(
    'browser', 'Browser MOD',
    'public', 'Public web',
    'about', 'About this WWW',
    'classic'
);

openSky(ROOM_DISPLAY);
medHeading('About WWW (restored)');
leaf("Archived WWW held early **shelf** and **browser chrome** ideas. It was factored old; this SYS brings them back on current routing (SYS/DOM/ROOM/MOD, /DOM/KEY, SKY_AUTH).");
leaf("Archive sources: a/--archive/DEMO/WWW, a/--archive/SDK-808/WWW, k/kittens/webBAR.kitten.js — still in --archive for reference.");
closeSky();
