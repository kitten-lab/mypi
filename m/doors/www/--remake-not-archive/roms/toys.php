<?php
SKY__AUTH(
    'browser', 'Browser MOD',
    'roms', 'ROMs',
    'toys', 'Toy / ROM reader',
    'classic'
);

openSky(ROOM_DISPLAY);
medHeading('ROM reader (stub)');
leaf("ROMs are rooms-in-rooms. Later: getRom('postBASIC', 'RomStyle') opens a modal app. Archive had roms/games doors under DEMO/WWW.");
leaf("For now this is a placeholder room so the nav tree is real.");
closeSky();
